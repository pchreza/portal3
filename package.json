{
  "name": "portal3-assets",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for portal3 local CSS assets",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/tailwind.input.css -o ./assets/tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
