# شواهد اولیه ممیزی RTL/UI portal3

## صفحه ورود در viewport مرورگر

- مسیر آزموده‌شده: `http://127.0.0.1:8000/index.php`
- ریشهٔ سند صحیح است: `lang=fa` و `dir=rtl`.
- viewport گزارش‌شده: `1280×1100`.
- فرم ورود در مرکز صفحه با عرض حدود `374px` و ارتفاع حدود `227px` قرار دارد.
- body/main ارتفاع حدود `1100px` دارند؛ این ارتفاع با viewport فعلی هم‌راستاست، اما رفتار در viewportهای کوتاه و موبایل هنوز باید جداگانه آزموده شود.
- دکمهٔ تغییر تم در مختصات فیزیکی چپ-بالا قرار دارد؛ این یک تصمیم قابل‌قبول فقط در صورتی است که گوشهٔ فیزیکی برای کنترل مستقل از RTL مستند شده باشد.
- دکمهٔ گزارش خطا در فیزیکی چپ-پایین قرار دارد؛ CSS هم‌زمان `left:1rem` و `inset-inline-end:1rem` دارد که در RTL یک تصمیم فیزیکی/منطقی ترکیبی و مستعد ناسازگاری است.
- در درخت focus، کنترل‌های پنهان مودال گزارش خطا نیز با `tabIndex=0` وجود دارند؛ چون عناصر دارای `display:none` هستند فعلاً قابل‌مشاهده نیستند، اما بازشدن مودال نیازمند focus trap و بازگرداندن focus به FAB است.
- ورودی نام کاربری و رمز عبور `dir=rtl` و text-align طبیعی دارند؛ ورودی موبایل و OTP در کد با `dir=ltr` جدا شده‌اند.
- صفحهٔ ورود بدون حساب نصب‌شده قابل مشاهده است و پاسخ HTTP آن `200` است.

## نشانه‌های کدی مهم

- `assets/portal-ui.css:7-18` برای سایدبار دسکتاپ از `right/left:auto` فیزیکی استفاده می‌کند.
- `assets/portal-ui.css:347` برای toast از `left:50%` همراه با `translateX(-50%)` استفاده می‌کند؛ مرکز فیزیکی است و الزاماً مشکل نیست، ولی باید از نظر حرکت/animation RTL بررسی شود.
- `assets/portal-ui.css:360-374` برای FAB گزارش خطا، `left`، `right` و `inset-inline-*` را هم‌زمان override می‌کند.
- `includes/functions/helpers.php:550-553` برای toggle از `right:2px` و `translate:-24px` استفاده می‌کند؛ منطق فیزیکی باید به logical positioning یا یک تصمیم مستند RTL تبدیل شود.
- `includes/layout/admin_header.php:47-51` و `customer_header.php:51-55` جای منوی هدر را با `left-1/2`, `start-4`, `end-4` و transform فیزیکی تعیین می‌کنند؛ متن/دکمه‌های طولانی و چنددکمه‌ای باید در عرض‌های میانی تست شوند.
- `admin/sidebar.php:8-14` و `customer/sidebar.php:6-11` برای موبایل `padding-top:3.5rem` و منوی fixed از راست استفاده می‌کنند، اما overlay، بستن با Escape، focus trap و بازگرداندن focus در کد دیده نمی‌شود.
- `customer_header.php:80-86` dropdown اعلانات را با `left:0` باز می‌کند؛ در shell RTL معمولاً باید inline-end/سمت مناسب تصمیم‌گیری شود و در موبایل احتمال خروج از viewport بررسی شود.

این فایل در ادامه با یافته‌های سایر صفحات و breakpointها تکمیل می‌شود.

## داشبورد مدیریت در viewport دسکتاپ

- مسیر آزموده‌شده: `admin/index.php` با حساب مدیر آزمایشی.
- سایدبار در سمت راست قرار گرفته و ترتیب بصری منو با RTL سازگار است؛ با این حال، در viewport دسکتاپ باریک‌تر از فضای معمول برای برچسب‌های فارسی است و باید در عرض‌های میانی نیز بررسی شود.
- داشبورد کارت‌های آماری، جدول مشتریان اخیر و دسترسی سریع را در یک چیدمان سه‌ستونه/دو ناحیه نشان می‌دهد؛ در محتوای فارسی، کارت‌های دسترسی سریع ارتفاع‌های متفاوت و تراکم متنی بالایی دارند و نیازمند آزمون با برچسب‌های طولانی‌تر هستند.
- در هدر، عنوان صفحه در سمت چپ shell و نام مدیر در سمت چپ بالا قرار دارد؛ این تصمیم با RTL لزوماً نادرست نیست، اما گروه‌بندی عنوان/عملیات/هویت کاربر باید در یک direction matrix صریح شود.
- جدول مشتریان دارای مقادیر فارسی، شماره موبایل و تاریخ شمسی است؛ شماره موبایل در خروجی جدول جدا از `dir=ltr`/`bdi` دیده می‌شود و برای ترکیب برچسب فارسی و شماره باید به‌صورت ایزوله آزموده شود.
- در screenshot، dashboard به‌طور کلی بدون clipping دیده شد؛ شواهد موبایل و breakpointهای کوچک هنوز جمع‌آوری نشده‌اند.

## صفحهٔ مدیریت مشتریان

- جدول داخل `.table-scroll` قرار دارد و در دسکتاپ عرض جدول `958px` و عرض wrapper `958px` گزارش شد؛ در viewport فعلی overflow افقی ندارد.
- ترتیب ستون‌ها از راست به چپ با متن فارسی سازگار است، اما ستون عملیات در انتهای فیزیکی چپ قرار گرفته و عرض آن `241px` است؛ این ستون نسبت به داده‌های اصلی بسیار عریض است و در عرض‌های کوچک احتمالاً فضای زیادی مصرف می‌کند.
- مقدار شماره موبایل در DOM با `dir=ltr` دیده شد اما `text-align:right` باقی مانده است؛ برای دادهٔ عددی/تلفنی بهتر است alignment و isolation با سیاست مشخص تست شود.
- headerهای جدول `white-space:nowrap` هستند؛ روی موبایل فقط در صورت فعال‌شدن `.table-card-mobile` به کارت تبدیل می‌شوند، بنابراین هر route باید وجود این کلاس را تضمین کند.
- عملیات ردیفی «ویرایش/حذف» دو کنترل متنی در یک سلول هستند؛ در موبایل باید بررسی شود که حداقل target لمسی و wrap مناسب باقی بماند.
- دکمه‌های خروجی Excel و نمونهٔ فایل در toolbar افقی با متن فارسی‌اند؛ در عرض‌های میانی احتمال فشرده‌شدن toolbar و برخورد با دکمهٔ «وارد کردن» وجود دارد.

## فرم افزودن مشتری

- فرم در دسکتاپ دو ستون با عرض `435px` برای هر ستون و فاصلهٔ بین‌ستونه‌ای قرار گرفته است؛ ترتیب داده‌ها در DOM و ترتیب بصری باید در موبایل به یک ستون منطقی تبدیل شود.
- در نمای فعلی، «نام کاربری» در ستون راست و «رمز عبور» در ستون چپ قرار گرفته است؛ این ترتیب برای فرم RTL می‌تواند منطقی باشد، اما در صورت stack شدن باید ترتیب focus و ترتیب DOM با سناریوی ورود هم‌راستا بماند.
- ورودی شماره موبایل در شواهد با `dir=rtl` دیده شد و text-align آن `start` است؛ این یک ریسک مستقیم برای نمایش، cursor و copy/paste شماره تلفن است و باید `dir=ltr` یا `dir=auto` با alignment مناسب داشته باشد.
- ورودی تاریخ شمسی حدود `297px` و دکمهٔ انتخاب تاریخ `130px` در یک ردیف قرار دارند؛ در عرض‌های کوچک یا با ترجمه/فونت متفاوت احتمال فشرده‌شدن یا overflow وجود دارد.
- دکمه‌های «انصراف» و «ذخیره اطلاعات مشتری» در فیزیکی چپ فرم قرار دارند؛ لازم است order معنایی CTA اصلی و secondary در RTL مستند و در keyboard order تأیید شود.
- متن دکمه‌ها `white-space:nowrap` دارند؛ برای فرم‌های فارسی طولانی، در mobile باید wrap یا stacking کنترل‌شده وجود داشته باشد.

## صفحات پروژه‌ها و فرم تعریف پروژه

- صفحهٔ فهرست پروژه‌ها در نمای دسکتاپ empty state را به‌صورت قابل‌فهم نشان می‌دهد، اما جدول خالی همچنان headerهای خود را نگه می‌دارد؛ برای mobile باید مشخص شود آیا empty state داخل جدول کارت‌سازی می‌شود یا نه.
- فرم تعریف پروژه به سه بخش شماره‌دار تقسیم شده است و برای فارسی خوانا است، اما فرم در viewport فعلی حدود `196px` محتوای خارج از viewport دارد؛ این ذاتاً خطا نیست، ولی باید sticky action/دسترسی به CTA در انتهای فرم بررسی شود.
- فیلد جست‌وجوی مشتری، select مشتری و عنوان پروژه به متن‌های ترکیبی فارسی/لاتین/شماره موبایل وابسته‌اند؛ گزینهٔ select شامل `audit_customer` و `09121111111` است و نیازمند isolation و نمایش بدون جابه‌جایی punctuation است.
- کنترل تاریخ تکمیل پروژه با input و دکمهٔ مجزا در یک ردیف قرار گرفته است؛ همان ریسک فشرده‌شدن controlها در عرض کوچک وجود دارد.
- بخش آپلود تصویر، متن فرمت‌ها و محدودیت `۴MB` دارد؛ مقدارهای فنی مانند `png / jpg / webp / svg` و `۴MB` باید در یک run ایزوله یا با سیاست عدد/لاتین مستند نمایش داده شوند.
- عنوان بخش‌ها و CTAها از emoji/نمادهای متنی استفاده می‌کنند؛ این نمادها باید از نظر baseline، screen reader announcement و consistency در dark mode بررسی شوند.

## صفحات فاکتورها

- صفحهٔ فهرست فاکتورها empty state را با headerهای «شماره/عنوان»، «مشتری»، «مبلغ»، «سررسید»، «وضعیت» و «عملیات» نشان می‌دهد؛ این ساختار در حالت داده‌دار باید از نظر alignment عدد و وضعیت تست شود.
- فرم صدور فاکتور مقدار پیش‌فرض `INV-2026-0001` و placeholder مبلغ `۵,۰۰۰,۰۰۰` دارد. شناسهٔ فاکتور یک مقدار فنی/لاتین است و باید `dir=ltr` و selectable/copyable باشد؛ مبلغ نیز باید سیاست واحد برای ارقام فارسی/لاتین و جداکننده داشته باشد.
- فیلد «سررسید پرداخت» مانند سایر تاریخ‌ها با input و دکمهٔ تقویم جداست و در عرض کوچک ریسک فشردگی دارد.
- وضعیت فاکتور با select فارسی ارائه شده است؛ در صورت استفاده از badge در جدول، باید رنگ تنها وسیلهٔ انتقال وضعیت نباشد و کنتراست dark mode نیز بررسی شود.
- دکمهٔ «ذخیره فاکتور» و «انصراف» در فیزیکی چپ کارت قرار دارند؛ ترتیب CTA و keyboard order باید با الگوی واحد فرم‌ها هماهنگ شود.

## تیکت‌ها و تنظیمات سیستم

- صفحهٔ تیکت‌ها toolbar فیلتر متراکم دارد: جست‌وجو، وضعیت، اولویت، دپارتمان، فیلتر، پاک‌کردن و checkbox همگی در یک ناحیهٔ افقی قرار گرفته‌اند. در screenshot دسکتاپ فشرده است و در موبایل احتمال wrap نامنظم، کوچک‌شدن selectها و گم‌شدن checkbox وجود دارد.
- empty state تیکت‌ها پیام «هیچ تیکتی مطابق فیلترها یافت نشد» دارد و راهنمایی تغییر فیلتر/پاک‌کردن را نشان می‌دهد؛ این پیام برای حالت «داده‌ای وجود ندارد» و «نتیجه‌ای مطابق فیلتر نیست» دو معنای متفاوت دارد و بهتر است stateهای empty و no-results جدا شوند.
- صفحهٔ تنظیمات نوار تب‌های افقی دارد و در screenshot یک scrollbar افقی قابل مشاهده است؛ ترتیب تب‌ها از راست به چپ نمایش داده می‌شود، اما discoverability تب‌های سمت چپ و keyboard/scroll affordance آن‌ها نیازمند اصلاح است.
- تب‌های تنظیمات با برچسب‌های نسبتاً طولانی مانند «فیلدهای اجباری»، «داشبورد کاربر»، «ظاهر سایت» و «ورود و پیامک» در یک ردیف آمده‌اند و در عرض میانی احتمال clipping یا scroll پنهان وجود دارد.
- کارت ماژول‌ها از toggleهای مشترک استفاده می‌کند؛ چون implementation toggle در `helpers.php:550-553` از `right` و `translate` فیزیکی استفاده می‌کند، وضعیت روشن/خاموش آن در RTL باید با focus، لمس و animation صریحاً تست شود.

## نظرسنجی

- فهرست نظرسنجی empty state استاندارد دارد، اما ستون‌های «مرحله»، «سؤال» و «پاسخ» در حالت داده‌دار مفاهیم ترتیبی/عددی‌اند و نباید صرفاً با mirror کردن UI وارونه شوند.
- فرم ساخت نظرسنجی شامل عنوان، توضیحات، نوع فرم و checkbox دوره‌ای است؛ برچسب checkbox طولانی است و در mobile باید بدون clipping و با association صحیح label/control wrap شود.
- دکمهٔ «ذخیره و افزودن سؤال‌ها» یک flow مرحله‌ای ایجاد می‌کند؛ در صفحهٔ بعد باید تصمیم mirror شدن دکمه‌های قبلی/بعدی، ترتیب DOM و focus و نیز شمارهٔ سؤال‌ها روشن باشد.
- برای ratingهای ۱ تا ۱۰، کد پروژه در `survey-public.php` و `customer/surveys.php` از `direction:ltr` استفاده می‌کند. این می‌تواند برای ترتیب عددی یک تصمیم درست باشد، اما باید با توضیح «کمترین/بیشترین»، keyboard arrow keys و screen reader در locale فارسی اعتبارسنجی شود؛ صرفاً RTL بودن صفحه نباید مقیاس را وارونه کند.

## نشست و ورود مشتری

- ناوبری مستقیم به `index.php` هنگام وجود نشست مدیر به‌صورت خودکار به `admin/index.php` redirect می‌کند؛ این رفتار از نظر دسترسی درست است، اما برای کاربرانی که نقش/نشست تغییر می‌کند باید پیام یا مسیر بازگشت قابل‌فهم وجود داشته باشد.
- خروج از نشست آزمایشی مدیر به صفحهٔ ورود بازگشت و shell عمومی همان RTL پایه را حفظ کرد. بررسی کامل پنل مشتری در مرحلهٔ بعد انجام می‌شود.

## داشبورد مشتری

- داشبورد مشتری در RTL از سایدبار راست، header و کارت‌های آماری استفاده می‌کند و در viewport فعلی کلیت آن خوانا است.
- کارت‌های آماری با برچسب‌های طولانی مانند «نظرسنجی‌های در انتظار» و «فاکتورهای مالی» در grid قرار دارند؛ در عرض‌های کوچک باید ترتیب کارت‌ها و ارتفاع محتوای چندخطی بررسی شود.
- بنر خوشامدگویی گرادیانی با متن سفید و CTA درون آن دارد؛ کنتراست، line-height متن فارسی و جای CTA در dark/light theme باید با WCAG تست شود.
- در header مشتری، دکمهٔ اعلان در سمت چپ گروه کنترل‌ها است و dropdown آن در کد با `left:0` باز می‌شود؛ در RTL این placement ممکن است با لبهٔ viewport یا سایدبار برخورد کند، به‌ویژه روی موبایل.
- صفحه در screenshot حدود `34px` محتوای خارج از viewport پایین دارد؛ این مقدار کم است اما نشان می‌دهد footer/آخرین کارت‌ها باید در breakpointهای کوچک بدون clipping آزموده شوند.
- کارت‌های empty state سه‌گانهٔ پروژه، اعلان و محصول از الگوی مشابه استفاده می‌کنند؛ تفاوت معنایی empty و no-results باید برای هر ماژول در copy مشخص شود.

## پروژه‌ها و تیکت‌های مشتری

- صفحهٔ پروژه‌های مشتری در حالت empty state کارت ساده و خوانا دارد، اما هیچ preview یا راهنمای «چگونه پروژه اضافه می‌شود» ارائه نمی‌کند؛ این بیشتر مسئلهٔ UX/copy است تا RTL.
- صفحهٔ تیکت‌های مشتری جدول با ستون‌های موضوع، دپارتمان، اولویت، وضعیت، تاریخ ایجاد و مشاهده دارد؛ در حالت خالی پیام و CTA واضح است، اما وقتی موضوع/دپارتمان طولانی یا mixed-direction شود باید responsive card strategy و truncation بررسی شود.
- CTA «تیکت جدید» در سمت چپ محتوای اصلی دیده می‌شود؛ در RTL باید به‌عنوان trailing action یا action اصلی با position و focus order واحد در کل پنل تصمیم‌گیری شود.
- CSS مشترک chat thread از `.chat-row.mine { flex-direction: row-reverse; }` استفاده می‌کند؛ این برای تشخیص فرستنده ممکن است درست باشد، اما نباید با ترتیب زمانی پیام‌ها اشتباه شود و باید با نام کاربر/زمان/متن mixed-direction آزموده شود.

## نظرسنجی و پروفایل مشتری

- صفحهٔ نظرسنجی مشتری در حالت empty state پیام روشن دارد، ولی active survey flow در این ممیزی بدون دادهٔ واقعی بررسی نشد؛ بنابراین rating scale، validation و submit state باید در QA با fixture واقعی آزموده شوند.
- فرم پروفایل مشتری در دسکتاپ grid دو ستونه دارد و فیلد شماره موبایل با مقدار `09121111111` در نمای فعلی `dir=rtl`/alignment طبیعی به نظر می‌رسد؛ این برای تلفن ریسک cursor، selection و copy/paste دارد.
- فیلد نام کاربری مقدار لاتین `audit_customer` دارد، اما در screenshot/DOM بدون isolation صریح دیده می‌شود؛ باید `dir=ltr`، selectable و قابل copy باشد و مقدار آن نباید با label فارسی جابه‌جا شود.
- ردیف تاریخ تولد input و دکمهٔ «انتخاب تاریخ» را کنار هم قرار می‌دهد؛ روی mobile باید به wrap یا stack کنترل‌شده تبدیل شود.
- دکمه‌های «بازگشت» و «ذخیره تغییرات پروفایل» در لبهٔ چپ فرم هستند و دکمهٔ اصلی/فرعی باید در کل فرم‌ها یک الگوی RTL یکسان داشته باشند.
- فرم پروفایل دارای فیلدهای اجباری بالقوه است؛ پیام‌های validation و required marker باید کنار label، بدون اتکا به رنگ و بدون ارتفاع ثابت، در حالت متن طولانی بررسی شوند.

## یافتهٔ قطعی: clipping عنوان‌های پنل مشتری

- در `customer/profile.php:131` و `customer/notifications.php:38`، heading با کلاس Tailwind `h-3` تعریف شده است.
- اندازه‌گیری مرورگر نشان داد این headingها `font-size:20px` و `line-height:27px` دارند، اما ارتفاع واقعی عنصر فقط `12px` است؛ بنابراین عنوان فارسی عملاً در ارتفاع ثابت کوتاه‌تر از line-height خود قرار گرفته و مستعد clipping/overlap است.
- این مورد **High** است چون در دو route واقعی تکرار می‌شود و با حذف `h-3` یا جایگزینی با کلاس معنایی/بدون height ثابت باید اصلاح شود.

## حالت تاریک

- حالت تاریک با کلیک روی theme toggle فعال شد و کلاس `dark` روی root قرار گرفت؛ shell، سایدبار و empty state در screenshot به‌صورت کلی خوانا بودند.
- اندازه‌گیری computed style نشان داد متن اصلی `rgb(219,227,239)` روی پس‌زمینهٔ body `rgb(30,43,71)` و متن muted `rgb(159,176,201)` استفاده می‌کند؛ این ترکیب از نظر ظاهری مناسب است، اما contrast ratio رسمی باید با ابزار خودکار و رنگ‌های تمام badge/alertها اندازه‌گیری شود.
- FAB گزارش خطا در computed style رنگ متن سفید و background شفاف داشت؛ پس از استخراج inline style، این کنترل باید از نظر اینکه gradient واقعاً اعمال می‌شود و در dark mode مرز/کنتراست کافی دارد، به‌صورت جداگانه تست شود.
- active nav با رنگی از `color-mix` نمایش داده می‌شود؛ ترکیب رنگ فعال با متن سفید باید در paletteهای قابل تنظیم ادمین نیز بررسی شود، چون کاربر می‌تواند رنگ primary را تغییر دهد.
- در dark mode مشکل `h-3` همچنان پابرجاست و heading اعلان با `font-size:20px`، `line-height:27px` و ارتفاع واقعی `12px` باقی می‌ماند.

## نتایج smoke test پس از اصلاحات shared

- PHP lint روی تمام فایل‌های PHP خارج از vendor بدون خطا عبور کرد.
- heading اعلان مشتری پس از حذف `h-3` ارتفاع طبیعی حدود `25.1875px` با line-height حدود `25.2px` دارد و دیگر height ثابت 12px ندارد.
- مودال گزارش خطا در DOM دارای `aria-describedby`، سه close hook و panel با `tabindex=-1` است. بازکردن مودال body را lock می‌کند؛ بستن با close hook display را به none، overflow را به مقدار قبلی و focus را به `error-report-fab` بازگرداند.
- drawer مشتری با شبیه‌سازی viewport 390px بازشدن را با `aria-expanded=true`، حذف hidden، backdrop فعال و body lock نشان داد؛ Escape هر چهار حالت را به وضعیت بسته برگرداند. focus return در این شبیه‌سازی به‌دلیل بازگردانی مصنوعی innerWidth در پایان اندازه‌گیری قطعی نیست و در مرورگر واقعی موبایل باید یک بار بصری/تعامل‌محور تکرار شود.

## تأیید runtime فرم پروفایل

- بعد از اصلاح، heading «ویرایش اطلاعات شخصی» در runtime کلاس بدون height ثابت دارد و ارتفاع `25.1875px` با line-height `25.2px` گزارش شد؛ اختلاف اعشاری ناشی از rounding مرورگر است و clipping ثابت وجود ندارد.
- در desktop فقط یک `.desktop-form-actions` با display `flex` دیده شد و `.mobile-action-bar` به‌درستی `display:none` بود؛ بنابراین CTA تکراری در desktop وجود ندارد.
- username، mobile و birth date همگی `dir=ltr`، کلاس `value-ltr` و `unicode-bidi:isolate` دارند و برای copy/paste و ترتیب mixed-direction آماده‌اند.

## تأیید runtime dropdown اعلان

- پس از reload واقعی route، کنترل اعلان دارای `id="notif-bell-toggle"` و `aria-controls="notif-dropdown"` بود.
- با فعال‌سازی، `aria-expanded=true` و dropdown visible شد و `inset-inline-end:0` گزارش شد؛ با Escape، `aria-expanded=false` و dropdown hidden شد.
- این نتیجه نشان می‌دهد اصلاح R05 برای placement logical و کنترل keyboard در shell مشتری runtime شده است.

## تأیید runtime survey-public

- route عمومی `survey-public.php` بدون token با پاسخ قابل‌مشاهدهٔ «امکان تکمیل فرم وجود ندارد» load شد.
- heading اصلی کلاس `font-bold text-slate-900 leading-snug mb-2` و height `21px` برابر line-height `21px` داشت؛ clipping height ثابت حذف شده است.
- headingهای hidden مربوط به مودال‌ها height صفر دارند و به‌دلیل hidden بودن مشکل rendering محسوب نمی‌شوند.

## مرور بصری mobile login

- screenshot نهایی در viewport `390×844` نشان داد کارت ورود، labelها، inputها، CTA اصلی و footer بدون overflow یا clipping دیده می‌شوند.
- در اجرای اولیهٔ logical slot، FAB به سمت راست رفت؛ علت این بود که `inline-start` در RTL سمت راست است. اصلاح به `inline-end` آن را دوباره در لبهٔ فیزیکی چپ، دور از سایدبار راست، قرار داد. screenshot نهایی این placement را تأیید کرد.
