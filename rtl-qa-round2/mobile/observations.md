# یافته‌های آزمون responsive دور دوم

| صفحه | viewport | نتیجه | یافته |
| --- | --- | --- | --- |
| `admin/tickets.php` | 390×844 | نیازمند اصلاح | فرم فیلتر به‌درستی به یک ستون reflow شده و داده‌های جدول به کارت‌های برچسب‌دار تبدیل شده‌اند. بااین‌حال دکمهٔ منوی drawer در header موبایل در لبهٔ فیزیکی چپ قرار گرفته، در حالی‌که navigation-leading در RTL باید در inline-start (راست) باشد. |
| `admin/tickets.php` | 390×844 | PASS پس از اصلاح | منوی drawer اکنون در inline-start (راست) است؛ عنوان بدون هم‌پوشانی truncate می‌شود و فیلترها در یک ستون با touch targetهای قابل‌استفاده reflow شده‌اند. |
| `admin/notifications.php` | 390×844 | PASS برای reflow محتوا | فرم ارسال اعلان بدون clipping به یک ستون تبدیل شده، labelها و کنترل انتخاب تاریخ خوانا هستند. اسکرین‌شات اولیه پیش از اصلاح مشترک header گرفته شده و در بازتولید نهایی جایگزین می‌شود. |
| `admin/surveys.php` | 390×844 | PASS برای reflow جدول | جدول به کارت برچسب‌دار تبدیل می‌شود؛ عنوان مختلط، شمارنده‌های LTR و actionها بدون clipping قابل‌دسترسی‌اند. |
| `admin/customers.php` | 390×844 | PASS برای reflow جدول | username و موبایل با جهت LTR پایدار باقی می‌مانند؛ داده‌های هویتی/شرکتی با labelهای کارت نمایش داده می‌شوند و عملیات ویرایش/حذف wrap سالم دارند. |
| `customer/tickets.php` | 390×844 | PASS | drawer در inline-start قرار دارد؛ کارت‌های تیکت subject مختلط، تاریخ LTR و action مکاتبات را بدون overflow نمایش می‌دهند. |
| `customer/invoices.php` | 390×844 | نیازمند اصلاح | مبلغ، شناسه و تاریخ‌ها صحیح و LTR هستند؛ اما در کارت موبایل، شماره و عنوان فاکتور دو فرزند مستقیم cell هستند و عنوان در عرض بسیار کم به چند کلمهٔ عمودی می‌شکند. محتوا باید در یک wrapper واحد قرار گیرد. |
| `customer/invoices.php` | 390×844 | PASS پس از اصلاح | شماره و عنوان اکنون در wrapper واحد کارت قرار دارند؛ عنوان در یک خط خوانا می‌ماند و مقدار/تاریخ‌های LTR بدون جابه‌جایی نمایش داده می‌شوند. |
