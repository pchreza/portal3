# 🐞 گزارش کامل باگ‌های پروژه پورتال مشتریان (log.md)

> **تاریخ بررسی:** ۷ آگوست ۲۰۲۶
> **روش:** بررسی خط‌به‌خط ۴۳ فایل PHP (۴۳۰۸ خط) + schema.sql توسط ۵ ایجنت موازی + **تست زنده (curl)** روی PHP 8.4 + MariaDB برای تأیید هر باگ
> **علامت‌ها:** 🔴 بحرانی (امنیتی/خرابی) | 🟠 مهم (منطقی/عملکردی) | 🟡 متوسط | ⚪ جزئی
> **مستند:** ✅ = تأیید شده با تست زنده | 📄 = تأیید شده با بازبینی کد

---

## 📊 خلاصه آماری

| شدت | تعداد |
|---|---|
| 🔴 بحرانی | ۷ |
| 🟠 مهم | ۱۵ |
| 🟡 متوسط | ۱۴ |
| ⚪ جزئی | ۱۱ |
| **مجموع** | **۴۷** |

**بیشترین باگ‌ها در:** ماژول نظرسنجی (۱۲)، مدیریت تیکت (۸)، صفحات CRUD ادمین (۷)، نصب‌کننده (۶)، ماژول‌سازی/تنظیمات (۵)

---

## 🔴 باگ‌های بحرانی (امنیتی / خرابی کامل)

### B01 — XSS ذخیره‌شده (Stored XSS) در پنل ادمین ✅
- **فایل:** `admin/tickets.php` خط ۱۰۴ (`company_name`)، ۱۴۱ (`first_name`)، ۱۴۲ (`created_at`)
- **مشکل:** خروجی `company_name` و `first_name` بدون `htmlspecialchars` رندر می‌شود.
- **اثر:** مشتری می‌تواند در پروفایل خود `company_name` را با `<img src=x onerror=alert(1)>` ذخیره کند؛ وقتی ادمین صفحه تیکت را باز می‌کند کد اجرا می‌شود → **دزدیدن سشن ادمین، کنترل کامل سیستم**.
- **تأیید:** ✅ مقدار `<img src=x onerror=alert(1)>` به‌صورت raw در خروجی `admin/tickets.php?view=1` مشاهده شد.
- **راه‌حل:** پیچیدن همه خروجی‌ها در `htmlspecialchars()`.

### B02 — نصب‌کننده (install.php) بعد از نصب باز می‌ماند + بدون CSRF → ربایش سیستم ✅
- **فایل:** `install.php` (کل فایل)
- **مشکل:** هیچ گاردی برای «قبلاً نصب شده» وجود ندارد (`grep "نصب شده"` → ۰ نتیجه) و فرم‌های آن هیچ `csrf_token` ندارند.
- **اثر:** هر کسی (یا یک صفحه مخرب از طریق CSRF) می‌تواند `POST /install.php?step=1` بزند → `db_config.php` با دیتابیس خودش **بازنویسی** می‌شود و جداول بازسازی می‌شوند → **توقف کامل سرویس / از دست رفتن پیکربندی**.
- **تأیید:** ✅ `GET /install.php` بعد از نصب، HTTP 200 با ویزارد کامل برمی‌گرداند.
- **راه‌حل:** اگر `db_config.php` موجود بود → ریدایرکت و نمایش پیام؛ افزودن CSRF؛ گزینه قفل فایل نصب.

### B03 — اعتبارنامه دیتابیس در گیت کامیت شده 📄
- **فایل:** `db_config.php` (ردیابی‌شده در git: `git ls-files` → `db_config.php`)
- **مشکل:** نام کاربری/رمز دیتابیس واقعی در ریپوی عمومی/مشترک است.
- **اثر:** هر کسی با دسترسی به ریپو به دیتابیس شما متصل می‌شود.
- **راه‌حل:** افزودن به `.gitignore` + ارائه `db_config.example.php` + حذف از تاریخچه گیت (filter-branch/BFG).

### B04 — تزریق SQL در install.php از طریق نام دیتابیس 📄
- **فایل:** `install.php` خط ۱۹: `"CREATE DATABASE IF NOT EXISTS `{$db_name}` ..."` بدون escape کردن بک‌تیک.
- **اثر:** نام دیتابیس کنترل‌شده توسط کاربر با بک‌تیک می‌تواند کوئری دلخواه اجرا کند.
- **راه‌حل:** `str_replace('`', '``', $db_name)` یا اعتبارسنجی `[a-zA-Z0-9_]+`.

### B05 — خطای ۵۰۰ (Exception بدون catch) با ورودی نامعتبر ✅
- **فایل‌ها:** `admin/tickets.php` خط ۱۰-۱۹ (ریپلای با `ticket_id` نامعتبر → خطای FK) و `customer/tickets.php` خط ۱۱،۱۶ (ساخت تیکت با `priority` خارج از enum)
- **اثر:** `POST` با `ticket_id=99999` یا `priority=HACKED` → **HTTP 500** و افشای خطای PDO در حالت دیباگ.
- **تأیید:** ✅ هر دو حالت HTTP 500 برگرداندند.
- **راه‌حل:** اعتبارسنجی ورودی + try/catch سراسری.

### B06 — دور زدن وضعیت «بسته» تیکت ✅
- **فایل:** `customer/tickets.php` خط ۲۶-۴۳ و `admin/tickets.php` خط ۹-۱۹
- **مشکل:** UI فرم پاسخ را برای تیکت بسته مخفی می‌کند ولی سمت سرور هیچ چک `status !== 'closed'` ندارد.
- **اثر:** مشتری با POST مستقیم به تیکت بسته پاسخ می‌دهد → پیام ذخیره می‌شود **و تیکت دوباره باز می‌شود** (closed → open). ادمین هم می‌تواند پاسخ بدهد (status → answered).
- **تأیید:** ✅ پاسخ به تیکت بسته ذخیره شد و وضعیت از `closed` به `open` برگشت.
- **راه‌حل:** چک `$ticket['status'] !== 'closed'` در هر دو هندلر.

### B07 — DoS قفل ورود مبتنی بر IP/username ✅
- **فایل:** `includes/functions/auth.php` خط ۸-۱۹ + `index.php`
- **مشکل:** قفل با معیار `(username = ? OR ip_address = ?)` یعنی ۵ تلاش ناموفق با **یک نام کاربری** از هر IP، آن کاربر را قفل می‌کند؛ ۵ تلاش از یک IP هم کل آن IP را قفل می‌کند.
- **اثر:** مهاجم با ۵ تلاش ناموفق با نام کاربری قربانی، دسترسی او را ۱۵ دقیقه (یا بیشتر با ادامه تلاش‌ها) می‌بندد.
- **تأیید:** ✅ مشتری `customer1` به‌خاطر تلاش‌های ناموفق قبلی روی نام کاربری دیگر قفل شده بود (از همان IP).
- **راه‌حل:** قفل فقط بر اساس ترکیب دقیق (username AND ip) یا قفل username با احراز وجود کاربر.

---

## 🟠 باگ‌های مهم (منطق / عملکرد)

### B08 — پیام اشتباه هنگام قفل ورود ✅
- **فایل:** `index.php` خط ۲۷-۳۲
- **مشکل:** وقتی `login_is_locked()` درست است، `$error` ست می‌شود اما بلافاصله خط ۳۱ شرط `if ($error || ...)` پیام را با «لطفا نام کاربری و رمز عبور را وارد کنید.» **بازنویسی** می‌کند.
- **اثر:** کاربر قفل‌شده پیام واقعی را هرگز نمی‌بیند و گمراه می‌شود.
- **تأیید:** ✅ بعد از ۷ تلاش ناموفق، پیام نمایشی «لطفا نام کاربری...» بود نه پیام قفل.
- **راه‌حل:** جدا کردن شرط‌ها (اگر قفل است، پیام قفل را نمایش بده و ادامه نده).

### B09 — حذف در CRUDها خطای گمراه‌کننده + پیام دوتایی ✅
- **فایل‌ها:** `admin/customers.php` خط ۱۰-۲۴ و ۷۱-۷۸، `admin/projects.php` خط ۹-۱۹ و ۴۳-۵۰، `admin/products.php` خط ۹-۱۹ و ۴۱-۴۸، `admin/invoices.php` خط ۱۲-۲۲ و ۳۹-۴۶
- **مشکل:** فرم حذف (`action=delete`) اول از بلوک add/edit عبور می‌کند؛ فیلدهای فرم خالی می‌مانند → `$error` (مثلاً «نام کاربری الزامی است») ست می‌شود، سپس حذف انجام شده و `$success` هم ست می‌شود.
- **اثر:** بعد از هر حذف، **هم پیام موفقیت و هم پیام خطا** نمایش داده می‌شود + هشدارهای «Undefined array key» در لاگ.
- **تأیید:** ✅ بعد از حذف مشتری، هر دو پیام «مشتری با موفقیت حذف شد.» و «نام کاربری الزامی است.» هم‌زمان نمایش داده شدند.
- **راه‌حل:** هندل `action=delete` را قبل از بلوک add/edit بگذارید یا `elseif` کنید.

### B10 — «مربوط به:» در فرم تکمیل نظرسنجی همیشه خالی ✅
- **فایل:** `customer/surveys.php` خط ۱۶۶ (خروجی) — کوئری `$takeData` (خط ۹۲-۹۷) ستون `entity_title` را **نمی‌آورد**.
- **اثر:** در فرم پاسخ‌دهی نظرسنجی، «مربوط به:» همیشه خالی است (فقط در لیست درست است).
- **تأیید:** ✅ خروجی `مربوط به: ` خالی در `?take=ID`.
- **راه‌حل:** افزودن JOIN/الزاماً CASE به کوئری `$takeData`.

### B11 — ماژول‌های غیرفعال همچنان در داشبوردها و فرم‌ها ظاهر می‌شوند ✅
- **فایل‌ها:** `admin/index.php` خط ۵-۸ (آمار) و ۱۳۴-۱۵۷ (دسترسی سریع)، `customer/index.php` خط ۹۲-۱۴۰ (کارت‌ها) و ۸۶ (دکمه تیکت)، `admin/customers.php` خط ۱۷۲، `admin/projects.php` خط ۱۵۰، `admin/products.php` خط ۱۴۵ (فیلدهای سفارشی بدون چک ماژول)
- **مشکل:** `is_module_enabled()` فقط در منوها و خود صفحات بررسی می‌شود، نه در داشبورد/کارت‌های آمار/دکمه‌ها/فیلدهای سفارشی.
- **اثر:** تنظیمات صفحه «فعال‌سازی ماژول‌ها» ادعا می‌کند «از منو، منو کاربر و داشبوردها مخفی می‌شود» ولی کارت‌های آمار و لینک‌ها باقی می‌مانند (کلیک → ریدایرکت).
- **تأیید:** ✅ با `module_projects=0`، کارت «پروژه‌های فعال» در داشبورد ادمین همچنان نمایش داده شد.
- **راه‌حل:** بررسی ماژول در تمام خروجی‌های مربوط به ماژول.

### B12 — فیلدهای سفارشی مشتری هیچ‌وقت در پنل مشتری نمایش/ذخیره نمی‌شوند 📄
- **فایل:** `customer/profile.php` (فرم خط ۱۲۵-۱۸۷) — هیچ فراخوانی از `render_custom_fields_inputs('customer', $id, true)` یا `save_custom_fields_values('customer', $id)` نیست (با grep سراسری تأیید شد).
- **اثر:** قابلیت «نمایش در پنل مشتری» (`show_in_customer_panel`) و تابع رندر آن در کد وجود دارد اما **هیچ صفحه‌ای از آن استفاده نمی‌کند** → بخش منطقی که باید کار کند ولی نمی‌کند.
- **راه‌حل:** افزودن رندر+ذخیره فیلدهای سفارشی به پروفایل مشتری.

### B13 — مهاجرت‌ها (migrations.php) فقط در نصب اجرا می‌شوند؛ ارتقای نصب‌های موجود ممکن نیست 📄
- **فایل:** `migrations.php` — تنها فراخوانی: `install.php` خط ۹۶-۹۷.
- **اثر:** اگر کاربر روی هاست کد را آپدیت کند، ستون‌های جدید (مثل `entity_type`، `survey_assignments`...) ساخته نمی‌شوند و صفحات خطا می‌دهند. هیچ `upgrade.php` یا اجرای خودکار وجود ندارد (README ادعای «upgraded by migrations.php» را دارد اما مسیر آن فراهم نیست).
- **راه‌حل:** فراخوانی `portal_migrations($pdo)` بعد از اتصال در config یا اسکریپت upgrade.

### B14 — دو مسیر موازی و ناسازگار برای ساخت نظرسنجی 📄
- **فایل‌ها:** `admin/surveys.php` خط ۱۵-۲۷ (با چک «فقط یک فرم اولیه برای هر entity») و `admin/survey-create.php` (بدون این چک)
- **اثر:** از مسیر `survey-create.php` می‌توان چند «فرم اولیه» برای یک entity ساخت → داده‌های نظرسنجی ناسازگار؛ منطق دوره‌ای گیج می‌شود.
- **راه‌حل:** یک مسیر واحد با اعتبارسنجی مشترک.

### B15 — دکمه‌های «شروع نظرسنجی» در پروژه‌ها/محصولات بدون فیلتر `available_at` و بدون چک ماژول 📄
- **فایل‌ها:** `customer/projects.php` خط ۱۳-۱۶ و ۴۸، `customer/products.php` خط ۱۳-۱۶ و ۴۳
- **مشکل:** کوئری `survey_map` نه `is_module_enabled('surveys')` را چک می‌کند نه `sa.available_at <= NOW()`.
- **اثر:** وقتی ماژول نظرسنجی خاموش است دکمه‌ها نمایش داده می‌شوند؛ برای نظرسنجی دوره‌ایِ هنوز فعال‌نشده هم دکمه می‌آید و کلیک → خطای «هنوز فعال نشده است».
- **راه‌حل:** افزودن فیلتر به کوئری و چک ماژول.

### B16 — logout کوکی سشن را پاک نمی‌کند ✅
- **فایل:** `logout.php` خط ۳-۶
- **مشکل:** فقط `session_destroy()` (سمت سرور)؛ کوکی `PHPSESSID` در مرورگر باقی می‌ماند.
- **اثر:** باقی‌ماندن کوکی بی‌اعتبار + احتمال سوءاستفاده در سناریوهای session fixation.
- **تأیید:** ✅ کوکی بعد از logout در jar باقی ماند.
- **راه‌حل:** `setcookie(session_name(), '', time()-3600, ...)` + `$_SESSION = []` + `session_regenerate_id(true)` قبل از destroy.

### B17 — تغییر وضعیت با GET (`?skip=1`) در پروفایل 📄
- **فایل:** `customer/profile.php` خط ۸-۱۳
- **مشکل:** `profile.php?skip=1` یک تغییر حالت دیتابیس است که از طریق GET انجام می‌شود (بدون CSRF).
- **اثر:** قابل تحریک با `<img src="...profile.php?skip=1">` (CSRF) یا پیش‌کش مرورگر.
- **راه‌حل:** تبدیل به POST یا حداقل CSRF token در کوئری.

### B18 — حذف سؤال نظرسنجی با GET 📄
- **فایل:** `admin/survey-questions.php` (`if (isset($_GET['delete']))`)
- **اثر:** حذف رکورد از طریق GET — خطر CSRF/پیش‌کش/ربات‌ها.
- **راه‌حل:** فرم POST با CSRF.

### B19 — رمز پیش‌فرض ضعیف و بی‌صدا «123456» برای مشتریان جدید 📄
- **فایل:** `admin/customers.php` خط ۲۷-۲۹
- **اثر:** مشتریانی که بدون رمز ساخته می‌شوند رمز قابل حدس دارند و اطلاعی به ادمین داده نمی‌شود.
- **راه‌حل:** رمز تصادفی + نمایش یک‌باره + اجباری‌کردن تغییر.

### B20 — خطای CSRF با پاسخ 419 در auth ها؛ اما install بدون CSRF است 📄
- **فایل:** `admin/auth.php` و `customer/auth.php` — محافظت خوب، ولی `install.php` خارج از آن است (به B02 مراجعه شود).

### B21 — ستون‌ها/فیلدهای «مرده» در دیتابیس 📄
- **فایل:** `schema.sql` — `survey_questions.sort_order` (خط ۱۴۰)، `surveys.target_scope` و `surveys.target_id` (خط ۱۲۹-۱۳۰)
- **اثر:** ساخته می‌شوند اما هیچ‌جا استفاده نمی‌شوند (grep صفر نتیجه) → قابلیت‌های ناتمام/مرده.

### B22 — شرط همیشه-درست (tautology) در کوئری نظرسنجی 📄
- **فایل:** `includes/functions/surveys.php` خط ۳۱: `AND ((is_periodic = 0) OR is_periodic = 1)` → همیشه true. کد مرده/ناقص.

---

## 🟡 باگ‌های متوسط (UX / ساختار / داده)

### B23 — پنل ادمین هیچ منوی موبایلی ندارد 📄
- **فایل:** `admin/sidebar.php` خط ۵ (`hidden md:flex`) — در موبایل سایدبار کاملاً مخفی است و فقط داشبورد یک لینک «خروج» دارد.
- **اثر:** ادمین در موبایل **هیچ راهی برای ناوبری بین صفحات ندارد** (فقط از لینک‌های داشبورد).

### B24 — منوی موبایل مشتری بعد از کلیک بسته نمی‌شود 📄
- **فایل:** `customer/sidebar.php` خط ۶ — فقط toggle دستی.

### B25 — بدون `.gitignore` و فایل‌های اضافی در گیت 📄
- `db_config.php`، `uploads/Screenshot (43).png` و `(44).png` در ریپو ردیابی شده‌اند؛ هیچ `.gitignore` وجود ندارد.

### B26 — آمار داشبورد ادمین اشتباه/گمراه‌کننده 📄
- **فایل:** `admin/index.php` خط ۶ — برچسب «پروژه‌های فعال» اما `COUNT(*)` **همه** پروژه‌ها (شامل تکمیل‌شده) را می‌شمارد.

### B27 — پیام خطای فیلد سفارشی همیشه بازنویسی می‌شود 📄
- **فایل:** `admin/custom_fields.php` خط ۱۵-۲۱ — اگر نوع نامعتبر باشد، خط ۲۰ پیام را با «نام سیستمی و برچسب فیلد الزامی است» جایگزین می‌کند (کاربر دلیل واقعی را نمی‌بیند).

### B28 — عدم اعتبارسنجی مبلغ فاکتور (فقط متن) 📄
- **فایل:** `admin/invoices.php` خط ۱۷،۲۴ — `amount` بدون چک عددی؛ هر متنی ذخیره می‌شود.

### B29 — شماره فاکتور پیش‌فرض با `rand()` 📄
- **فایل:** `admin/invoices.php` خط ۱۰۶ — پیش‌فرض `INV-rand(1000,9999)` هر بار متفاوت و قابل حدس است.

### B30 — نبود محدودیت‌های یکتایی در اسکیما 📄
- `invoices.invoice_number` (تکراری مجاز)، `custom_fields.(target_entity, field_name)` (تکراری مجاز)، `custom_field_values.(field_id, entity_id)` (رکورد تکراری ممکن در race)
- **فایل:** `schema.sql` خطوط ۸۵، ۱۰۲-۱۱۱، ۱۱۳-۱۱۹

### B31 — نبود کلید خارجی روی `ticket_messages.sender_id` و `surveys.parent_survey_id` 📄
- **فایل:** `schema.sql` خط ۷۵ و ۱۲۷
- **اثر:** داده‌های یتیم؛ پیام‌های تیکت با INNER JOIN به users (admin/tickets.php خط ۵۵-۵۸) اگر کاربر حذف شود ناپدید می‌شوند.

### B32 — عملکرد: `ensure_survey_assignments` در هر بازدید اجرا می‌شود 📄
- **فایل:** `includes/functions/surveys.php` — N+1 کوئری در هر بازدید داشبورد/پروژه/محصول/نظرسنجی؛ `INSERT IGNORE` باعث می‌شود تغییر `delay_days` بعداً اعمال نشود.

### B33 — نمای تکراری/مرده در `admin/surveys.php` 📄
- متغیر `$action` (خط ۶) استفاده نمی‌شود؛ نمای `?questions=ID` و `?results=ID` در همان فایل، **تکرار** صفحه‌های جداگانه `survey-questions.php` و `survey-results.php` است (دو پیاده‌سازی موازی، فقط یکی لینک‌خورده).

### B34 — هشدارهای PHP روی POST های ناقص (لاگ‌آلودگی) 📄
- `trim($_POST['username'])` و مشابه بدون `?? ''` در `customers.php` خط ۱۲-۲۰، `projects.php` خط ۱۱-۱۶، `products.php` خط ۱۱-۱۶، `invoices.php` خط ۱۴-۱۹، `tickets.php` خط ۱۰-۱۱ → هشدارهای «Undefined array key»/Deprecated در POST های حذف.

### B35 — نداشتن صفحه تغییر رمز ادمین 📄
- هیچ صفحه/فرمی برای تغییر رمز عبور مدیر وجود ندارد (فقط نصب‌کننده). باگ امنیتی-عملیاتی.

### B36 — `public_survey.php` لینک قدیمی را بدون ورود به لاگین می‌فرستد 📄
- کاربری که لینک قدیمی نظرسنجی دارد به صفحه ورود می‌افتد (نبود پیام راهنما).

---

## ⚪ باگ‌های جزئی

1. **B37** — `install.php` متغیر `$success` را تعریف می‌کند ولی هیچ‌جا استفاده نمی‌کند (کد مرده).
2. **B38** — `customer/index.php` خط ۸۷: نام کامل خالی فقط یک فاصله نمایش می‌دهد (در جدول مشتریان اخیر ادمین هم همین).
3. **B39** — هیچ صفحه‌ای pagination ندارد؛ لیست‌ها (مشتریان، تیکت‌ها، لاگ‌ها...) بدون محدودیت رشد می‌کنند (`logs.php` فقط LIMIT 100 دارد).
4. **B40** — `admin/invoices.php` هیچ فیلد سفارشی/توضیحی برای فاکتور ندارد (ناسازگاری با بقیه ماژول‌ها).
5. **B41** — `admin/products.php` برخلاف `projects.php` جستجوی مشتری ندارد (ناسازگاری UX).
6. **B42** — `admin/tickets.php` خط ۱۴۱ نام فرستنده پیام‌های مشتری را از `$active_ticket` می‌خواند نه از `$msg` — اگر روزی تیکت مالک عوض شود، نام‌ها اشتباه می‌شود.
7. **B43** — `customer/surveys.php`: بعد از ثبت پاسخ ریدایرکت به `?submitted=1` اتفاق می‌افتد (خوب) ولی بقیه فرم‌های پروژه PRG ندارند → Refresh دوباره POST می‌کند (مثلاً ساخت تکراری مشتری/تیکت).
8. **B44** — `config.php` خط ۷: تشخیص HTTPS با `SERVER_PORT` — پشت پراکسی (nginx/Cloudflare) کوکی secure اشتباه ست می‌شود.
9. **B45** — `admin/settings.php`: سوییچ‌های toggle فقط ظاهری‌اند؛ کلاس `peer-checked:after:-translate-x-6` در RTL ممکن است جهت نادرست جابه‌جا شود (نیاز به تست بصری).
10. **B46** — `admin/tickets.php` خط ۱۱۰: `onchange="this.form.submit()"` — بدون `confirm`؛ تغییر ناخواسته وضعیت با یک کلیک.
11. **B47** — سشن شامل `$_SESSION['first_name']` قدیمی بعد از ویرایش توسط ادمین (مشتری) — تا logout به‌روز نمی‌شود.

---

## ✅ مواردی که درست است (برای اطمینان)

- Prepared Statements در تمام کوئری‌های صفحه (به‌جز install.php که با var_export و بک‌تیک سروکار دارد).
- محافظت CSRF مرکزی در `admin/auth.php` و `customer/auth.php` برای همه POST ها (به‌جز install).
- `password_hash`/`password_verify` برای رمزها.
- چک مالکیت تیکت در سمت مشتری (`customer/tickets.php` خط ۳۰، ۴۹).
- escape در اکثر خروجی‌های لیست‌ها (غیر از موارد B01).
- سشن با `httponly` و `samesite=Lax`.
- تست خودکار: ۲۶/۲۶ صفحه قبل و بعد از بازسازی سالم (200، بدون خطای PHP).

---

## پیشنهاد اولویت‌بندی رفع

| مرحله | باگ‌ها |
|---|---|
| **۱ (فوری)** | B01، B02، B03، B04، B05، B06، B07 |
| **۲ (عملکردی)** | B08، B09، B10، B11، B12، B13، B14، B15 |
| **۳ (تکمیلی)** | B16 تا B22 |
| **۴ (UX/ساختار)** | B23 تا B36 |
| **۵ (جزئی)** | B37 تا B47 |

> اگر بخواهید، از مرحله ۱ شروع می‌کنم و باگ‌ها را یک‌به‌یک برطرف می‌کنم و بعد از هر مرحله تست مجدد می‌گیرم.
